export function DemoSourceNotice() {
  return (
    <p className="text-xs leading-relaxed text-white/40">
      Real recorded swing footage and its measured output, replayed here as a demo recreation for the
      landing page — not a live in-browser analysis. Overlay graphics are illustrative, not a frame-by-
      frame tracking output.
    </p>
  )
}
